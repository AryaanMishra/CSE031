You can ignore the .vscode branch

//For Cloning
To clone the entire repo
  1. open terminal
  2. cd to ur CSE folder
  3. git init
  3. git clone https://github.com/AryaanMishra/CSE031.git

To clone specific branch from this git
  1. open terminal
  2. cd to ur CSE folder
  3. git init
  3. git clone --depth 1 --no-checkout https://github.com/AryaanMishra/CSE031.git
  2. cd CSE031
  3. git sparse-checkout set [INSERT_DESIRED_SUBDIRECTORY_RELATIVE-PATH] 
     //get the relative path by copying the path of the folder in github
  4. git checkout

//This is here just in case I forget the commands.
//For master
To update folders in CSE031
  1. git add.
  2. git commit -m "commit message"
  3. git push -u origin master
