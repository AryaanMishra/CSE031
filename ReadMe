//For Clone
To Clone specific branch from this git
  1. git clone --depth 1 --no-checkout https://github.com/AryaanMishra/CSE031.git
  2. cd CSE031
  3. git sparse-checkout set [INSERT_DESIRED_SUBDIRECTORY_RELATIVE-PATH]
  4. git checkout

//For master only
To update folders in CSE031
  1.
